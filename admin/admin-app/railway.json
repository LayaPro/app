{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "rm -rf node_modules .vite && npm ci --omit=dev && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s dist -l $PORT"
  }
}