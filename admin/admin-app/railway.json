{
  "build": {
    "buildCommand": "npm ci && rm -rf node_modules/.vite && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s dist -l 3000",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
