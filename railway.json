{
  "build": {
    "buildCommand": "cd packages/shared && npm ci && npm run build && cd ../../api/services && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "cd api/services && node dist/server.js"
  }
}
